<%= form_with url: sunshine_hours_url, method: :get do |form| %>
  <%= form.label :country, "Select country:" %>
  <%= form.select :country, @countries, selected: params[:country] || 'United States' %>
  <%= form.submit "Filter" %>
<% end %>
